import { Router } from "express";

const router = Router();

router.get("/",)
router.get("/login",)
router.get("/logout",)
router.get("/register",)


router.post("/login" , );
router.post("/register" ,);

export default router;